
import Link from "next/link";

export default function Logo({ }) {
  return (
      <Link href="/" className="-mt-2" title="Bloom">
         <h1 className="font-integralCF text-3xl font-bold uppercase">bloom</h1>
      </Link>
   );
}